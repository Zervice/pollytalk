export const downloadConfig = {
  organization: "Zervice",
  repo: "pollytalk",
  version: "1.1.3",
  apkName: "app-apk-release.apk",
};

export const downloadUrl = `https://github.com/${downloadConfig.organization}/${downloadConfig.repo}/releases/download/v${downloadConfig.version}/${downloadConfig.apkName}`;
